ws_client_homepage:
    path:     /
    defaults: { _controller: WSClientBundle:Default:index }

ws_client_logging:
    path:     /logging
    defaults: { _controller: WSClientBundle:Default:logging }
